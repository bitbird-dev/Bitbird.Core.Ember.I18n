import { Message } from 'dummy/utils/message';
import { module, test } from 'qunit';

module('Unit | Utility | message', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    //let result = Message.create();
    assert.ok(true);
  });
});
